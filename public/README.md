# Public assets

Place static files here that should be served at the root URL:

- `resume.pdf` — your downloadable CV (referenced from /about)
- `favicon.ico` — site favicon
- `/diagrams/` — architecture diagrams referenced from project frontmatter
- `/og/` — custom Open Graph images for posts that override the auto-generated ones
